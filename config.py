from datetime import date

# --- Savings goal -------------------------------------------------
CASH_GOAL = 50000        # target: checking + savings only
GOAL_MONTHS_MIN = 18
GOAL_MONTHS_MAX = 24
GOAL_SET_DATE = date.today()    # update this to when the goal was actually set, if known
